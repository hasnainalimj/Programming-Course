var text = "the war is going to hap!pen World War II";
var result = text.replace("World War II","The Second World War");

var text = "the war is going to happen World War II the war is going to happen World War II the war is going to happen World War II";
text = text.replace(/World War II/g,"The Second World War");

var num = 25.7;
num = Math.round(num)

var numbers = Math.random() // Intial Range 0 to Less than 1

var local = Math.random();
var mine = local * 5;

mine = Math.floor(mine);

nums = Math.ceil(nums);

var string = "Ali";
var num = 123;
var decimal = 12.5;

//Data types and conversion
num = num.toString();
num = parseInt(num);
decimal = parseInt(decimal);
decimal = parseInt(decimal)
string = parseInt(string);
decimal = parseFloat(decimal);

var num = 10.1254445;
num = num.toFixed(2)

var mystry = 7;
var input = + prompt();
if(input === mystry) {
console.log("You win");
}
else if(input+1 === mystry || input-1 === mystry){
console.log("close enough")
}


